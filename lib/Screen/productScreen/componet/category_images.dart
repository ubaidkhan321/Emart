
import 'package:e_seller_app/const/const.dart';

Widget categoryImages({label,onpress}){
  return "$label".text.size(16).color(fontGrey).makeCentered().box.color(lightGrey).roundedSM.size(100, 100).make();
}